# src/components — placeholder scaffold (Phase 2)

**Nothing lives here yet.** This directory is pre-scaffolded for the component library planned in [`architecture.md §5`](../../architecture.md#5-component-architecture).

When Phase 2 starts, components land here as **React + TypeScript + Radix UI** primitives styled with CSS variables (no Tailwind, no CSS-in-JS), one directory per component per `architecture.md §5.3`:

```
Button/
├── Button.tsx
├── Button.types.ts
├── Button.stories.tsx
└── index.ts
index.ts            # barrel export for all components
```

Do not add components until Phase 2 begins (see [`roadmap.md`](../../roadmap.md)).
