# CLAUDE.md

This repo's agent guidance lives in **[`AGENTS.md`](AGENTS.md)** — read it first, then **[`memory.md`](memory.md)** for the full working context (current state, locked decisions, known doc drift, open questions).

**TL;DR:** `standardly-ds` is the design system for **Standardly** (compliance-intelligence platform). It is **tokens-only (Phase 1)** — no library build yet; `src/` and `.storybook/` are empty placeholder scaffolds (READMEs only) for Phase 2+. **Current focus: Sprint S1** (`explorations/` — 3 design-direction demos for the 2026-07-14 team call; see `roadmap.md` + `tasks.md`). `tokens.json` (W3C DTCG) is canonical and stays at repo root. Single light theme (`$themes: []`, no dark mode). Brand primary is **`purple`** (locked; `brief.md` was rewritten to match). Keep `memory.md` updated when you learn something non-obvious.
