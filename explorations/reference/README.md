# reference/ — S1-T02 source screenshots

The two source images were provided in chat on 2026-07-10 and could not be written to disk from there. **Naman: drop them in here as:**

- `prototype-dashboard.png` — the current Standardly prototype's dashboard ("Welcome back, Naman!" page: top nav, 4 stat cards, Quick Actions, Recent Activity, Getting Started callout). This page defines the binding component inventory in [`../README.md`](../README.md).
- `direction-o1-link-ai.png` — the Link AI copilot dashboard used as the primary design-language reference for direction **o1 — Soft Precision** (soft large-radius cards, pill controls, muted label hierarchy, one-accent discipline).

Until the files land, the written inventory + direction definition in `../README.md` are the working record of what the images contain.
